# Assouan SOCKS5 Proxy Socket

SOCKS5 proxy socket transport for the A PHP libraries.

```bash
composer require assouan/socks5-proxy-socket
```

Requires PHP 8.5 or later.

## Classes

- `A\Proxy\Socks5ProxySocket`
